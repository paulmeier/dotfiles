# Allowlist: this PUBLIC repo tracks only Doom, zsh and Starship config.
# Everything else is ignored by default, so nothing lands here by accident.
# To track something new, add a `!` rule below and make sure it's secret-free.
/*
!/.gitignore
!/.stow-local-ignore
!/README.md
!/.zshrc
!/.config/
/.config/*
!/.config/doom/
!/.config/zsh/
!/.config/starship.toml

# Doom: paths to my data
/.config/doom/local.el

# Never, anywhere
.DS_Store
*~
\#*\#
.\#*
*.elc
.authinfo*
.env
.env.*
*.local
